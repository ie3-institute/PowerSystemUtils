// OneJar is plugin for building self contained JARs (JARs with all needed dependencies)

tasks.register('selfContainedJar', OneJar){
	mainClass = mainClass
	archiveName = 'simona_selfcontained.jar'
}
